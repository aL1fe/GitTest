https://git-scm.com/book/ru/v2

git help <команда>
git <команда> --help //Документация по команде
git <команда> -h //Посмотреть только список команд
----------------------------
git config --global --list //вывести настройки
git config --global user.name "My Name"
git config --global user.email myEmail@example.com
git config --global core.editor "'C:/Program Files/Notepad++/notepad++.exe' -multiInst -notabbar -nosession -noPlugin" //установка notepad++ в качестве редактора поумолчанию
----------------------------
git init //инициализация локального репозитория
dotnet new gitignore //создать gitignore для начала проекта
----------------------------
git status //просмотреть состоятние Index
git status -s //сокращенный просмотр состоятние Index
----------------------------
git add -A //добавить в Index весь репозиторий 
git add . //добавить все содержимое текущей папки и ее подпапок в Index
git add File01.txt <имя_файла> <имя_файла> //добавить отдельные файлы
----------------------------
git commit //создать коммит, откроется редактор, первая строка название коммита, строки ниже коментарии
git commit -m "My commit message" //создать коммит c именем My commit message

//Исправить коммит если что-то забыли добавить
git commit -m 'Commit with mistake'
git add <forgotten_file>
git commit --amend  //Данная команда затрет предыдущий комит новым
----------------------------
//Работа с ветками (ветка это всего лишь указатель на коммит)
git branch <branchname> //создать новую ветку с именем branchName
git checkout -b <branchname> //создать новую ветку с именем branchName и переключиться в нее

git checkout <branchname> //выкачать ветку branchName в свой локальный репозиторий
git checkout <commit_hash> //выкачать коммит SHA в свой локальный репозиторий

git branch -D <branchname> //удалить ветку branchName навсегда
git branch -d <branchname> //закрыть ветку branchName(можно потом ее оживить)

git branch -m newName //переименовать текущую ветку
git branch -m oldName newName //переименовать ветку из любой ветки

git cherry-pick <hash>  //скопировать в текущую ветку коммит с указанным хешом

git merge <branchname>  //слить текущую ветку с веткой branchname

git checkout <commit_hash>  //переключиться на старый коммит (установить HEAD на коммит с нужным хешем)
----------------------------
git log  //log текущей ветки
git log --all //log всех веток
git log master //вывести лог ветки master
git log --max-count=3 master //вывести последние 3 лога
git log --oneline --decorate  //сокращенный log текущей ветки
----------------------------
//Припрятывание
git stash  //припрятать изменения
git stash list  //вывести список вариантов припрятанных изменений
git stash apply stash@{x}  //вернуть изменения с именем stash@{x}
git stash drop stash@{x}  //удалить припрятанные изменения с именем stash@{x}
----------------------------
//Операции отмены
git reset HEAD <имя_файла> <имя_файла> <имя_файла> //отмена индексации файла
 
git reset --hard HEAD //отменить изменения во всех файлах включая проиндексированные (загрузить файлы из последнего коммита)
git reset --hard <hash> //резетнуть ветку до нужного хеша (загрузить файлы из коммита суказанным хешом)
git reset --hard HEAD^1 //удалить последний коммит, вернуться к состоянию предыдущего коммита

git checkout <имя_файла> //отменить изменения в отдельном файле даже если он проиндексирован (выкачать файл из коммита)

git checkout-index <имя_файла> //вернуть удаленный файл из индекса
git checkout-index -a //вернуть все удаленные файлы из индекса

git restore --staged <имя_файла> //отменить изменения в index
git restore --staged * //отменить изменения в index все файлы

git restore <имя_файла> //отменить изменения в worktree
git restore * //отменить изменения в worktree все файлы
---------------------------
git diff //сравнить worktree с index
git diff --staged //сравнить index с последним коммитом
---------------------------
git log --oneline --decorate --graph //графическое отображение log коммитов текущей ветки
git log --oneline --decorate --graph --all  //графическое отображение log коммитов всех веток
---------------------------
//Работа с удаленным репозиторием
git remote -v //посмотреть адреса для чтения и записи, привязанные к репозиторию

git ls-remote 		  //посмотреть ветки в удаленном репоизитории
git ls-remote origin  //---"---
git remote show origin //ветки в удаленном репоизитории и дополнительная информация

git fetch		  //извлечь данные для текущей ветки с удаленного репозитория в область commit
git fetch origin  //---"---
git fetch --all //извлечь данные для всех веток с удаленного репозитория в область commit

git push --set-upstream origin <localBranch>  				 //создать на сервере ветку с именем localBranch и сделать ее веткой слежения
git push --set-upstream origin <localBranch>:<originBranch>  //создать на сервере ветку с именем originBranch и сделать ее веткой слежения для localBranch

git push origin <localBranch>				  //отправить изменения из локальной ветки localBranch в удаленную ветку с таким же именем (создать ее) БЕЗ слежения
git push origin <localBranch>:<originBranch>  //отправить изменения из локальной ветки localBranch в удаленную ветку originBranch (создать ее) БЕЗ слежения

git branch -u origin/<originBranch> //настроить текущую ветку на слежение за удалённой веткой originBranch
git branch -vv //посмотреть настройки веток слежения


---------------------------
git rm -r -f .vs //добавить в gitignore папку .vs
git rm -r -f .idea  //---"--- .idea
---------------------------
При работе с Git может возникнуть такая ситуация: команда git push не заливает изменения на сервер, а команда git status показывает:
$ git status
HEAD detached from 87dc87b
Это может означать, что вы делали какую-то навигацию по истории коммитов, и неправильно вернулись к последнему коммиту.
Исправить эту проблему можно 4-мя командами:
git branch temp
git checkout temp
git branch -f master temp
git checkout master
И, опционально,
git branch -d temp
---------------------------
